name: CI Server
on:
  push:
    branches:
    - main
    
env:
  IntegrationType: Jira_Server
  
jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        fetch-depth: 1
        clean: false
        
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '3.1.x'
      
    - name: xml-replace-action
      uses: rvolo/xml-replace-action@v0.3
      with:
        filepath: '${{ github.workspace}}/.github/workflows/test.xml'
        xpath: '/Project/PropertyGroup/Version/text()'
        replace: '${{ github.run_number}}'
        
    - name: Restore dependencies
      run: dotnet restore ./ShoppingSystem/
               
    - name: Build
      run: dotnet build --configuration Release --no-restore ./ShoppingSystem/
      
    - name: Test
      run: dotnet test --no-restore --no-build --logger 'junit;LogFilePath=.github/workflows/testresult.xml' --configuration Release ./ShoppingSystem/
      
    - name: Publish Unit Test Results
      uses: EnricoMi/publish-unit-test-result-action/composite@v1
      if: always()
      with:
        files: .github/workflows/testresult.xml
        
    - uses: papeloto/action-zip@v1
      with:
        files: .github/workflows/test.xml
        dest: result.zip
        
    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v2.2.3
      with:
        name: myartifact
        path: ${{ github.workspace }}/result.zip
